"""
测试 Step1 新旧 prompt 输出对比
"""
import os
import sys
import io
sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding='utf-8')
sys.path.insert(0, os.path.dirname(__file__))

from services.deepseek_service import call_deepseek

# BP 文本
bp_text = open(
    r"D:\复旦文件\Semester3-4\搞事情\论文产品化\投资助手\workspace\杉海创新科技6_20260422_162546\parsed\bp_text.txt",
    encoding="utf-8"
).read()[:8000]

company_name = "杉海创新科技6"
industry = "先进材料"

# ============ 旧版 Prompt ============
OLD_SYSTEM_PROMPT = """你现在是一位一级市场投资分析师，请对输入的项目材料进行"通用理解分析"。
你的目标不是复述BP，而是：用投资人的视角，把这个项目"讲清楚"，为后续结构化分析提供基础。

【你的任务】
请输出以下6个部分（必须按结构输出）：

一、【公司本质（One-liner）】
- 用一句话说明：这家公司本质上在做什么
- 要去掉包装（例如：不是"AI平台"，而是"XX业务公司"）

二、【业务结构拆解】
请拆解公司当前业务（按重要性排序）：
对每一条业务说明：
- 做什么产品/服务
- 客户是谁
- 怎么赚钱
- 当前是否已经产生收入

三、【收入与增长来源】
回答：
- 当前收入主要来自哪里
- 增长来自哪里（老业务放量 / 新业务 / 新市场）
如果数据不完整，请标记"不确定"

四、【发展阶段判断】
请判断公司当前处于哪个阶段（必须选择或组合）：
- 实验室 / 技术验证
- 小规模商业化
- 已规模化收入
- 多业务扩张阶段
并说明理由（基于事实）

五、【核心能力判断】
回答：
- 公司最核心的能力是什么（最多2条）
- 这些能力目前体现在哪里（产品 / 客户 / 技术）
避免泛化（如"技术很强"）

六、【初步风险扫描】
列出3-5个"第一眼风险"，例如：
- 信息缺失
- 逻辑不清
- 过度叙事
- 阶段错配
要求：
- 不需要深入分析
- 只做"提示性标记"

【重要要求】
- 不要复述BP原话
- 可以使用"更像 / 本质是 / 大概率"等投资人语言
- 允许标注"不确定"，不要编造
- 输出要为后续结构化分析服务（清晰、可引用）
"""

# ============ 新版 Prompt（用户提供 v2） ============
NEW_SYSTEM_PROMPT = """你现在不是分析助手，而是一位有经验的一级市场投资人。

你的任务不是整理信息，而是在阅读项目材料后，
给出你"第一轮直觉判断"，像在和合伙人快速讨论一个项目。

⚠️重要原则：
- 优先做判断，而不是覆盖全部信息
- 主动拆解叙事（尤其是AI、平台、行业、人设包装）
- 可以不完整，但必须有方向
- 不要写成研究报告或BP复述

请严格按照以下结构输出：

----------------------------------

一、【这家公司本质上是什么 / 不是什么】
- 用一句话说明公司本质
- 必须做减法（例如：是XX公司，不是XX公司）
- 去掉AI/平台/生态等包装性表述

----------------------------------

二、【它现在真正靠什么赚钱（只看已发生的）】
- 当前真实收入来自哪里
- 哪些业务已经被验证
- 哪些还只是"在长出来"或"讲故事"

⚠️不要把未来业务和当前收入混在一起

----------------------------------

三、【客户到底为什么付钱（最关键）】
- 客户为哪个"具体价值"付钱（只能写1-2个）
- 必须用客户视角表达（例如：更好卖/更稳定/更便宜/更容易做产品）
- 不要写技术原理

----------------------------------

四、【真正的壁垒 vs 最可能被高估的点】

1）真正可能的壁垒（最多2条）
- 只写已经体现出商业价值或验证路径的能力
（例如：工艺、客户验证、供应链、数据闭环等）

2）最可能被高估/包装的点（最多2条）
- 必须主动识别叙事包装，包括但不限于：
  - AI能力（是否只是工具）
  - 平台化/多行业扩展（是否提前讲故事）
  - 市场空间/国家战略（是否无关当前业务）
  - ⚠️人设/背书包装（重点）：
    如诺奖团队、院士、博士、专家顾问等
    判断这些是否真正转化为：
    - 排他性技术
    - 实际产品
    - 商业化能力
    否则应视为潜在包装

----------------------------------

五、【我最不信/最想验证的3个点】
- 只写3个最关键的怀疑点
- 必须具体，直接指向"这家公司可能被高估或判断错误的地方"
- 不要泛泛而谈（如"市场风险""竞争激烈"）

----------------------------------

六、【当前直觉倾向（非最终结论）】

- 当前更让我担心的是（1-2条）：
- 当前更让我觉得有价值的是（1-2条）：

⚠️说明：
- 这是初始直觉，不是最终投资判断
- 可以使用："我更倾向于认为 / 大概率 / 需要再看"
- 不要强行下结论（不需要写投或不投）

----------------------------------

【写作要求】

- 可以有主观判断，不需要绝对客观
- 不要覆盖所有信息，允许忽略次要内容
- 不要写成长篇报告
- 输出要像"投资人脑子里的第一版判断"

"""

def build_user_prompt(bp_text, company_name, industry):
    header = f"请分析以下"
    if company_name:
        header += f"关于「{company_name}」"
    if industry:
        header += f"（{industry}行业）"
    header += "的项目材料：\n\n"
    return header + bp_text


print("=" * 60)
print("【Step1 对比测试】杉海创新科技6")
print("=" * 60)

print("\n>>> 正在调用：旧版 Prompt...")
old_result = call_deepseek(
    system_prompt=OLD_SYSTEM_PROMPT,
    user_prompt=build_user_prompt(bp_text, company_name, industry)
)

print("\n>>> 正在调用：新版 Prompt...")
new_result = call_deepseek(
    system_prompt=NEW_SYSTEM_PROMPT,
    user_prompt=build_user_prompt(bp_text, company_name, industry)
)

# 保存结果
with open(r"D:\复旦文件\Semester3-4\搞事情\论文产品化\投资助手\workspace\杉海创新科技6_20260422_162546\step1_v1_old.txt", "w", encoding="utf-8") as f:
    f.write(old_result)

with open(r"D:\复旦文件\Semester3-4\搞事情\论文产品化\投资助手\workspace\杉海创新科技6_20260422_162546\step1_v2_new.txt", "w", encoding="utf-8") as f:
    f.write(new_result)

print("\n[OK] 结果已保存到项目目录")
print(f"   旧版v1: step1_v1_old.txt ({len(old_result)} chars)")
print(f"   新版v2: step1_v2_new.txt ({len(new_result)} chars)")
